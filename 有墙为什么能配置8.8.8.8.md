这是一个非常非常常见的误解！你把两个完全不同的“服务”搞混了。

这个问题的核心答案是：

**`8.8.8.8` 是谷歌的 “DNS 解析服务”，而不是 “谷歌网站本身”。**

**你访问不了谷歌，是访问不了 `www.google.com` 这个 \*网站服务\* (Web Service)；但你（通常）可以访问 `8.8.8.8` 这个 \*DNS 服务\* (DNS Service)。**

防火墙（GFW）的主要工作是**阻断**你和谷歌 *网站服务器* 之间的连接，但它并**没有（完全）阻断**你和谷歌 *DNS 服务器* 之间的连接。

------



### 1. 什么是DNS？（408知识点：应用层）



我们用一个比喻来解释：

- **DNS 服务器 (如 `8.8.8.8`)**：就像是 114 查号台（电话簿）。
- **域名 (如 `www.baidu.com`)**：是人名（“百度公司”）。
- **IP 地址 (如 `39.156.66.10`)**：是电话号码。

你的电脑要上网，必须进行这个动作： “嘿，`8.8.8.8`（查号台），请告诉我‘百度公司’（`www.baidu.com`）的电话号码（IP）是多少？”

`8.8.8.8` 会回答你：“它的号码是 `39.156.66.10`。”

然后，你的电脑**才会**去拨打 `39.156.66.10` 这个号码，从而连接上百度的网站。



### 2. 为什么配置 `8.8.8.8` 会“有效”？



你把 DNS 配置成 `8.8.8.8`，你的电脑会发生以下行为：

- **你想访问 `www.bilibili.com`（B站）：**
  1. **[DNS查询]** 你的电脑向 `8.8.8.8` 发送一个请求：“B站的IP是啥？”
  2. **[DNS响应]** `8.8.8.8` 回答你：“是 `118.24.111.160`。” （这个过程是**有效**的，防火墙没拦）
  3. **[HTTP连接]** 你的电脑**直接**去连接 `118.24.111.160`（B站的服务器）。
  4. **结果：** B站访问成功。

在这个过程中，你只是**利用** `8.8.8.8` 这个“查号台”帮你查了**B站**的号码。你从头到尾都没有访问谷歌的网站。



### 3. 为什么你访问不了谷歌，但 `8.8.8.8` 却有效？



现在，我们试试用 `8.8.8.8` 去访问谷歌网站：

- **你想访问 `www.google.com`（谷歌）：**
  1. **[DNS查询]** 你的电脑向 `8.8.8.8` 发送一个请求：“谷歌网站的IP是啥？”
  2. **[DNS响应]** `8.8.8.8` 忠实地回答你：“是 `142.250.191.196`。”（这个DNS查询过程本身依然是**有效**的！）
  3. **[HTTP连接]** 你的电脑尝试去连接 `142.250.191.196`（谷歌的网站服务器）。
  4. **[被阻断]** **防火墙（GFW）在这一步出手了**。它检测到你想连接的目标 `142.250.191.196` 是被禁止的，于是切断了这个连接。
  5. **结果：** 谷歌网站访问失败。

**结论：**

- 你访问不了谷歌，是因为防火墙阻断了**步骤 3（HTTP 连接）**。
- 配置 `8.8.8.8` 有效，是因为它能正常完成**步骤 2（DNS 响应）**，它能帮你查到**所有**网站的IP（包括B站、百度）。



### 4. `8.8.8.8` 和 `114.114.114.114` 的区别



既然 `8.8.8.8` 有效，为什么还要用 `114.114.114.114`？

- `8.8.8.8` (Google DNS)：服务器在**国外**。你每次“查号”（DNS查询）都要跨越太平洋，虽然能查到，但**延迟高**，速度慢，且这个“查询”过程本身也可能受国际出口线路波动影响，导致不稳定。
- `114.114.114.114` (国内公共DNS)：服务器在**国内**（南京）。你“查号”在本地就能完成，**延迟低**，速度快，非常稳定。

**总结：** `8.8.8.8` 和 `114.114.114.114` 都是“查号台”（DNS）。`8.8.8.8` 也能用，只是因为它是一个**基础服务**（DNS），防火墙的主要目标是**网站服务**（HTTP/HTTPS），而不是（完全）封锁DNS。

但在国内，使用 `114.114.114.114` 或你运营商（电信/联通/移动）自动分配的DNS，通常会比用 `8.8.8.8` **更快、更稳定**。