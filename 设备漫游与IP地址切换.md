这是一个非常经典的企业网络场景！用408（计算机网络）的知识来解释，这个过程涉及了数据链路层、网络层和应用层的协同工作。

核心结论是：**你抱着设备从房间A走到房间B，设备（的网卡）自动完成了从AP-A到AP-B的“漫游”（Roaming）。而IP地址发生改变，是因为AP-A和AP-B被划分在了两个不同的IP子网（Subnet）中。**

下面是这个过程中发生的详细技术分解，完全对应408的知识点：



### 1. 基础网络架构（408知识点：VLAN、子网划分、AP）



在一个大型覆盖区域（如你公司）中，为了网络管理和安全，通常不会把所有设备都放在同一个巨大的局域网（广播域）中。网络管理员会使用**子网划分（Subnetting）**和**VLAN（虚拟局域网）**技术。

- **场景推测：**
  - 房间A的无线接入点（AP-A）被划分在 `VLAN 10`，对应的IP子网是 `Subnet A`（例如 `192.168.10.0/24`）。
  - 房间B的无线接入点（AP-B）被划分在 `VLAN 20`，对应的IP子网是 `Subnet B`（例如 `192.168.20.0/24`）。
- 尽管这两个AP广播的是同一个Wi-Fi名称（SSID，例如 "MyCompany_WiFi"），但它们在数据链路层（二层）和网络层（三层）上是隔离的。
- `ip_a` (例如 `192.168.10.100`) 是 `Subnet A` 里的一个地址。
- `ip_b` (例如 `192.168.20.150`) 是 `Subnet B` 里的一个地址。

------



### 2. IP地址改变过程的逐步分解



这个过程可以分为三个阶段：



#### 阶段一：在房间A (持有 `ip_a`)



1. **连接与获取IP (应用层 & 网络层)**
   - 你的设备（比如笔记本电脑）在房间A打开，连接到信号最强的 `AP-A`。
   - 设备在 `VLAN 10` 中发送一个 **DHCP Discover** 报文（广播）。
   - 网络中的DHCP服务器（或DHCP中继）收到请求，发现它来自 `VLAN 10` / `Subnet A`。
   - DHCP服务器从 `Subnet A` 的地址池中拿出一个地址 `ip_a`，连同子网掩码、**默认网关 (Gateway-A)** 和DNS服务器地址等信息，通过 **DHCP Offer** 和 **DHCP Ack** 报文回送给设备。
   - 此时，设备IP配置为 `ip_a`，网关为 `Gateway-A` (例如 `192.168.10.1`)。



#### 阶段二：行走过程 (数据链路层漫游)



1. **Wi-Fi漫游 (L2 Roaming) (数据链路层)**
   - 你抱着设备走向房间B。设备网卡持续监测Wi-Fi信号。
   - 它发现 `AP-A` (房间A) 的信号强度（RSSI）在减弱，而 `AP-B` (房间B) 的信号在增强。
   - 当信号达到某个阈值时，网卡决定切换AP。
   - 设备向 `AP-B` 发送**关联请求 (Association Request)**。
   - `AP-B` 响应**关联成功 (Association Response)**。
   - （如果网络配置了802.1X等安全认证，这里还会有个认证过程）。
   - 设备与 `AP-A` **解除关联 (Disassociation)**。

这个从AP-A切换到AP-B的过程，在408中称为**二层漫游（L2 Roaming）**。从用户的角度看，Wi-Fi图标可能只是闪一下，甚至无感知。



#### 阶段三：进入房间B (IP切换为 `ip_b`)



1. **检测网络变化 (网络层)**
   - 设备现在关联到了 `AP-B`。`AP-B` 属于 `VLAN 20`。
   - 设备在物理上（二层）已经进入了一个全新的广播域。
   - 设备有两种方式发现这一点：
     - **主动发现：** 设备尝试使用 `ip_a` 在新网络中通信。例如，它发送一个ARP请求给它的旧网关 `Gateway-A`。但由于 `Gateway-A` 和设备已不在同一个子网/VLAN，ARP请求将失败（收不到ARP响应）。设备会判定网络已更改。
     - **被动发现：** 设备收到了来自 `AP-B` 或新网络（`VLAN 20`）的广播/组播包（例如路由通告），发现这些包的源IP与自己的子网不符。
2. **重新获取IP (应用层 & 网络层)**
   - 一旦设备确认它在 一个新的网络上，它会**立即**（或者在 `ip_a` 的租约到期后）重新启动DHCP过程。
   - 设备在 `VLAN 20` 中发送一个新的 **DHCP Discover** 报文（广播）。
   - DHCP服务器（或中继）收到这个请求，发现它来自 `VLAN 20` / `Subnet B`。
   - DHCP服务器从 `Subnet B` 的地址池中选择一个地址 `ip_b`，连同新的**默认网关 (Gateway-B)** (例如 `192.168.20.1`) 等信息，回送给设备。
   - 设备更新自己的IP配置，IP地址从 `ip_a` 变为 `ip_b`，默认网关从 `Gateway-A` 变为 `Gateway-B`。

------



### 总结：涉及的408核心知识点



这个简单的“走路换IP”过程，完美地串联了408计算机网络的多个章节知识：

1. **物理层 & 数据链路层 (IEEE 802.11 Wi-Fi):**
   - AP（无线访问点）的作用。
   - SSID（服务集标识符）。
   - 802.11的关联（Association）和漫游（Roaming）过程。
2. **数据链路层 (交换):**
   - **VLAN (802.1Q):** 核心技术。它在逻辑上隔离了AP-A和AP-B，使它们处于不同的广播域。
   - 交换机（Switch）根据VLAN Tag来转发数据帧。
3. **网络层 (IP):**
   - **IP地址与子网划分:** `ip_a` 和 `ip_b` 属于不同网络前缀的地址。
   - **默认网关 (Default Gateway):** 设备必须通过网关才能访问其他子网。IP切换时，网关地址也必须随之切换。
   - **路由器 (Router) / 三层交换机:** 负责在 `Subnet A` 和 `Subnet B` 之间进行路由转发（即让房间A和房间B的设备能互相访问）。
4. **应用层:**
   - **DHCP (动态主机配置协议):** 整个“自动切换”IP的核心。设备通过DHCP Discover/Offer/Request/Ack 过程动态获取IP地址、子网掩码和网关。

**简而言之：** 你的设备在二层（数据链路层）上从一个AP漫游到了另一个AP；因为这两个AP在三层（网络层）上被划分在不同的IP子网，所以设备必须通过DHCP重新获取一个属于新子网的IP地址，才能正常通信。