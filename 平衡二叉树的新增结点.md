你抓到的这个问题，正是平衡二叉树（Balanced Binary Tree, 简称 BBT）的灵魂，也是408考研数据结构的**绝对重点和难点**。

我们通常所说的“平衡二叉树”在408中特指**AVL树**（以其发明者G. M. **A**delson-**V**elsky 和 E. M. **L**andis 命名）。

你说的“较大变动”就是指插入后可能导致的**“失衡” (Imbalance)** 以及后续的**“再平衡” (Rebalancing)** 过程。这个过程的核心就是**「旋转」 (Rotation)**。

我们来彻底梳理一遍这个过程。

------



### 核心概念：什么是“平衡”？



AVL树的平衡依赖于一个核心指标：**平衡因子 (Balance Factor, 简称 BF)**。

- **$BF = \text{结点左子树的高度} - \text{结点右子树的高度}$**
- **AVL树规定：** 树中**所有**结点的 $BF$ 只能是 **-1、0 或 1**。
- 一旦某个结点的 $BF$ 变成了 **+2** 或 **-2**，就意味着该结点“失衡”了。



### 插入的全过程：从插入到再平衡



当你向AVL树新增一个结点时，会发生以下连锁反应：



#### 步骤一：执行普通二叉排序树 (BST) 插入



- 和普通BST一样，从根结点开始比较，小了往左走，大了往右走，直到找到一个`NULL`位置，把新结点插进去。
- 新插入的结点一定是**叶子结点**。



#### 步骤二：回溯，更新平衡因子 (BF)



- 这是关键的第一步。从**新插入的结点**开始，**沿着路径往回（向父结点）**走，一直走到根。
- 在回溯路径上，每经过一个结点，都要**更新它的 $BF$**。
- **为什么？** 因为你给它的一侧（左或右）增加了一个新结点，它的子树高度可能（但不一定）增加了 1。



#### 步骤三：寻找「最小失衡子树」



- 在回溯的过程中，你**遇到的第一个 $BF$ 变为 +2 或 -2 的结点**，我们称它为 $A$。
- 以 $A$ 为根的子树，就是**「最小失衡子树」** (Minimum Unbalanced Subtree)。
- **注意：** 我们只需要处理这个“最小”的失衡。408的重要结论是：**只要把这个最小失衡子树调整平衡了，整个AVL树就全部恢复平衡了。**



#### 步骤四：诊断失衡类型并执行「旋转」



- 这就是你说的“较大变动”的核心。
- 根据 $BF$ 的状态和新结点插入的位置，失衡分为四种类型，必须对症下药。

------



### 核心：四种失衡类型的诊断与旋转



假设我们找到的“最小失衡结点”是 $A$（它的 $BF$ 变成了 +2 或 -2）。



#### 诊断方法



诊断的依据是：**新插入的结点 $N$** 到底插在了 $A$ 的**哪棵孙子树**上？

1. **LL (左-左) 型**

   - **诊断：** $N$ 插在了 $A$ 的**左孩子 ($B$)** 的**左子树**上。
   - **失衡表现：** $A$ 的 $BF = +2$，$B$ 的 $BF = +1$。（因为左边变高了）
   - **变动（旋转）：** 对 $A$ 进行**「右单旋转」**。
   - **过程：**
     1. $B$ 提上来，成为新的根。
     2. $A$ 降下去，成为 $B$ 的右孩子。
     3. $B$ 原来的右子树（$B_R$），“甩”给 $A$ 作为 $A$ 的左子树。

   ```
     (A)+2          (B)0
     / \            / \
   (B)+1 (T3)  -> (T1) (A)0
   / \                / \
   ```

(T1) (T2)          (T2)(T3)

(新结点在T1)

\```

1. **RR (右-右) 型**

   - **诊断：** $N$ 插在了 $A$ 的**右孩子 ($B$)** 的**右子树**上。
   - **失衡表现：** $A$ 的 $BF = -2$，$B$ 的 $BF = -1$。
   - **变动（旋转）：** 对 $A$ 进行**「左单旋转」**。
   - **过程：** （与LL对称）
     1. $B$ 提上来，成为新的根。
     2. $A$ 降下去，成为 $B$ 的左孩子。
     3. $B$ 原来的左子树（$B_L$），“甩”给 $A$ 作为 $A$ 的右子树。

   ```
   (A)-2            (B)0
   / \              / \
   ```

(T1) (B)-1  ->   (A)0 (T3)

/ \         /

(T2)(T3)    (T1)(T2)

(新结点在T3)

\```

1. **LR (左-右) 型**

   - **诊断：** $N$ 插在了 $A$ 的**左孩子 ($B$)** 的**右子树**上。
   - **失衡表现：** $A$ 的 $BF = +2$，$B$ 的 $BF = -1$。
   - **变动（旋转）：** **「先左后右」双旋转**。
   - **过程：** 设 $B$ 的右孩子是 $C$。
     1. **步骤1 (先左)：** 对 $B$ 进行**左旋转**。（*此时 $C$ 成为 $B$ 的父结点*）
     2. **步骤2 (后右)：** 对 $A$ 进行**右旋转**。（*此时 $C$ 成为 $A$ 的父结点*）
   - **结果：** $C$ 成为新的根，$B$ 成为 $C$ 的左孩子，$A$ 成为 $C$ 的右孩子。

   ```
     (A)+2          (A)+2           (C)0
     / \            / \             / \
   (B)-1 (T4)  -> (C)+1 (T4)   ->  (B)0 (A)0
   / \            / \             / \  / \
   ```

(T1) (C)        (B) (T3)       (T1) (T2)(T3)(T4)

/ \        /

(T2)(T3)   (T1)(T2)

(新结点在T2或T3)

\```

1. **RL (右-左) 型**

   - **诊断：** $N$ 插在了 $A$ 的**右孩子 ($B$)** 的**左子树**上。
   - **失衡表现：** $A$ 的 $BF = -2$，$B$ 的 $BF = +1$。
   - **变动（旋转）：** **「先右后左」双旋转**。
   - **过程：** （与LR对称）设 $B$ 的左孩子是 $C$。
     1. **步骤1 (先右)：** 对 $B$ 进行**右旋转**。
     2. **步骤2 (后左)：** 对 $A$ 进行**左旋转**。
   - **结果：** $C$ 成为新的根，$A$ 成为 $C$ 的左孩子，$B$ 成为 $C$ 的右孩子。

   ```
   (A)-2            (A)-2           (C)0
   / \              / \             / \
   ```

(T1) (B)+1   -> (T1) (C)-1    -> (A)0 (B)0

/ \            / \            / \  /

(C) (T4)       (T2)(B)        (T1)(T2)(T3)(T4)

/ \                /

(T2)(T3)           (T3)(T4)

(新结点在T2或T3)

\```

------



### 408 考研关键结论



1. **插入最多旋转一次：** AVL树的插入操作，在回溯时**最多**只需要进行**一次旋转**（单旋转或双旋转）。一旦在“最小失衡子树”上完成了旋转，该子树的高度就恢复到了**插入前的高度**，因此从该子树往上的所有祖先结点的 $BF$ 都会恢复原状，**不需要继续向上检查或旋转**。
2. **删除可能多次旋转：** (与插入对比) AVL树的**删除**操作，在调整后可能会导致**父结点**也失衡，因此删除操作在最坏情况下，需要**一路回溯到根**，进行 $O(\log n)$ 次旋转。
3. **复杂度：** AVL树的查找、插入、删除操作，由于其高度始终被S限制在 $O(\log n)$，因此这三个操作的时间复杂度都是 $O(\log n)$。
4. **旋转的目的：** 旋转的本质是：将“太高”的子树（例如LL中的$B$的左子树）“提”到上一层，同时将“太矮”的子树（例如LL中的$A$的右子树$T_3$）“降”到下一层，从而使 $A$ 结点恢复平衡。