你这个问题提得非常专业，这正是408考研中的一个重要知识点！

确实有这样一类协议，它们**不使用 TCP 或 UDP**，而是“另起炉灶”，直接运行在 **IP 协议之上**。

这是因为它们的功能非常特殊，既不完全符合 TCP 的“重量级”可靠连接模型，也不符合 UDP 的“尽力而为”数据报模型。它们通常是**网络控制类**或**管理类**协议，而不是用来传输用户数据的应用层协议。

在 IP 头部中，有一个8位的字段叫做**“协议” (Protocol)**。这个字段就是用来指明 IP 包的数据部分应该交给谁来处理：

- 如果值是 **6**，IP 包就交给 **TCP**。
- 如果值是 **17**，IP 包就交给 **UDP**。
- 如果值是**其他数字**，IP 包就交给其他协议。

你所说的“另起炉灶”的协议，就是在这里使用了**自己的专属协议号**。



### 梳理：直接运行在 IP 上的协议 (非TCP/UDP)



以下是408中你必须掌握的几个核心协议：



#### 1. ICMP (Internet Control Message Protocol) - 协议号 1



- **中文名：** 互联网控制报文协议。
- **核心功能：** 它是IP协议的“管家”和“侦察兵”。它不传输用户数据，而是用来**报告网络错误、诊断网络状况**。
- **对“正确性”的要求：** 它必须能**正确地**传达网络中发生的错误信息。
- **为什么“另起炉灶”？**
  - **无法使用TCP：** TCP本身就是一个复杂的、可靠的协议，它依赖一个“健康”的网络来建立连接。而ICMP的工作恰恰是在网络**出问题时**（如路由不通、主机不可达）去报告问题。让一个报告网络错误的协议去依赖一个需要网络健康的协议，这在逻辑上是行不通的。
  - **不适合UDP：** UDP是“尽力而为”，发出去就不管了。而ICMP报文（如“目标不可达”）是网络基础设施的重要反馈，虽然它也不保证100%送达，但它需要比普通UDP数据更高的“存在感”，它是网络层自身的一部分。
- **典型应用：**
  - **Ping (Packet Internet Groper):** 使用 ICMP 的“回显请求” (Echo Request) 和“回显应答” (Echo Reply) 报文。
  - **Traceroute:** 使用 ICMP 的“TTL超时” (Time Exceeded) 报文。
  - **错误报告：** 如“目标主机不可达”、“端口不可达”、“需要分片但禁止分片”。



#### 2. IGMP (Internet Group Management Protocol) - 协议号 2



- **中文名：** 互联网组管理协议。
- **核心功能：** 用于**IP组播 (Multicast)**。它允许一台主机（如你的电脑）告诉本地的路由器：“嘿，我想加入/离开这个组播组（比如一个IPTV频道）”。
- **对“正确性”的要求：** 路由器必须能**正确地**知道它所连接的网段上有哪些主机对哪些组播感兴趣，否则它就会错误地转发或停止转发组播数据流。
- **为什么“另起炉灶”？**
  - 这是一种主机和路由器之间的**信令（Signaling）**协议。
  - 它不需要 TCP 的连接状态和流量控制，因为通信模型很简单（就是“加入”或“离开”的报告）。
  - 它也不适合 UDP，因为它是一种网络层面的控制协议，用于管理网络如何转发数据，而不是应用层的数据传输。它和ICMP一样，是IP协议的“辅助”协议。



#### 3. OSPF (Open Shortest Path First) - 协议号 89



- **中文名：** 开放最短路径优先协议。
- **核心功能：** 一种非常重要的**路由协议**（内部网关协议，IGP）。路由器之间用它来交换“链路状态信息”，以便每个路由器都能计算出整个网络的拓扑图，并找到最短路径。
- **对“正确性”的要求：** **极高！** 路由信息是网络的核心，如果路由信息交换出错或不可靠，整个网络就会瘫痪、产生路由环路。
- **为什么“另起炉灶”？**
  - **不能用UDP：** OSPF需要交换复杂的网络拓扑信息，这个过程必须是**可靠的**。UDP的“不可靠”特性完全无法满足需求。
  - **为什么不用TCP：**
    1. **开销问题：** OSPF需要在所有相邻路由器之间都维持连接，如果使用TCP，每个连接都需要维护一套完整的TCP状态机（序列号、窗口、定时器），开销非常大。
    2. **控制权问题：** OSPF是一个底层的路由协议，它希望**自己完全控制**数据的发送时机、确认机制和重传策略。例如，OSPF有自己的Hello机制来维持邻居关系，有自己的LSA（链路状态通告）更新和确认机制。
    3. **效率问题：** 它不想受制于TCP的拥塞控制算法，它需要更快速地收敛。
  - **结论：** OSPF干脆绕过TCP/UDP，**在IP之上自己实现了一套“轻量级”的可靠传输机制**（包括序列号、确认和重传），专门用于高效、可靠地交换路由信息。

------



### 总结



对于408考试，你需要记住：

| **协议** | **协议号 (IP)** | **核心功能**              | **为什么“另起炉灶”**                            |
| -------- | --------------- | ------------------------- | ----------------------------------------------- |
| **ICMP** | 1               | 网络诊断、差错报告 (Ping) | 报告网络层错误，不能依赖传输层                  |
| **IGMP** | 2               | IP组播管理                | 网络层的主机-路由信令，非数据传输               |
| **OSPF** | 89              | 路由协议 (链路状态)       | 需要高度可靠，但TCP开销太大，故**自建可靠机制** |

(顺便提一下，另一个路由协议 **EIGRP** 也一样，使用IP协议号 **88**，也是自己实现了可靠传输。)