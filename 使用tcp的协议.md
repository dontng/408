没问题，我们用同样的风格来梳理**必须使用 TCP** 的协议。

TCP (Transmission Control Protocol) 的核心特性是 **面向连接（Connection-oriented）** 和 **可靠传输（Reliable）**。

因此，所有使用 TCP 的应用层协议，它们的**共同需求**是：**数据的完整性和顺序性** 远比 **速度和低延迟** 更重要。它们无法忍受数据包的丢失、重复或乱序，因为这会导致整个应用逻辑的失败（如文件损坏、命令错乱、邮件不完整）。

------



### 梳理：使用 TCP 的协议



我们可以把这些协议分为三大类：



#### 类别一：“零差错”类（文件与网页传输）



这类应用的核心需求是**数据必须 100% 完整且正确**。哪怕只是为了重传一个丢失的字节而导致应用卡顿几秒，也必须等待，因为一个字节的错误就可能导致整个文件或页面无法使用。

1. **HTTP (HyperText Transfer Protocol)** - 端口 80
   - **核心功能：** 访问万维网（World Wide Web）页面。
   - **为什么用TCP：** 你浏览的网页是由 HTML、CSS、JavaScript 文件和图片组成的。这些文件必须**完整无误**地下载到你的浏览器。如果 HTML 代码丢失了一部分，整个页面布局可能完全错乱；如果 JavaScript 丢了代码，网页功能会失效。
2. **HTTPS (HTTP Secure)** - 端口 443
   - **核心功能：** 加密版的 HTTP，用于安全访问（如网上银行、登录）。
   - **为什么用TCP：** 它和 HTTP 的需求一样，都需要可靠传输。它只是在 TCP 之上、HTTP 之下增加了一个 SSL/TLS 加密层，但底层的可靠性依然由 TCP 保证。
3. **FTP (File Transfer Protocol)** - 端口 20 (数据), 21 (控制)
   - **核心功能：** 文件传输。
   - **为什么用TCP：** 这是最典型的“可靠性”需求。无论你下载的是一个软件、一个文档还是一个压缩包，你都必须保证下载的文件和服务器上的文件**一模一样（bit-by-bit identical）**。如果用 UDP，文件传下来基本肯定是损坏的。



#### 类别二：“状态同步”类（邮件与会话）



这类应用不仅要传输数据，还要在客户端和服务器之间**维护一个清晰的会话状态**。数据的顺序至关重要。

1. **SMTP (Simple Mail Transfer Protocol)** - 端口 25
   - **核心功能：** **发送**电子邮件。
   - **为什么用TCP：** 保证你写的邮件能完整地、不多不少地从你的邮箱客户端传送到邮件服务器，再传到对方的服务器。
2. **POP3 (Post Office Protocol 3)** - 端口 110
   - **核心功能：** **下载**电子邮件。
   - **为什么用TCP：** （就是你之前问的那个）保证你从服务器收取邮件时，邮件正文和附件能完整下载到本地，不会出现“附件损坏”或“正文乱码”。
3. **IMAP (Internet Message Access Protocol)** - 端口 143
   - **核心功能：** 在服务器上**管理**邮件（比POP3更高级）。
   - **为什么用TCP：** 当你在多个设备上（如手机和电脑）读邮件时，IMAP 需要可靠地同步“已读”、“删除”、“移动到文件夹”等状态，这必须是可靠操作。
4. **SSH (Secure Shell)** - 端口 22
   - **核心功能：** 安全的远程命令行登录。
   - **为什么用TCP：** 你的每一次键盘输入（比如一个命令 `ls -l`）都必须**按顺序**、**完整**地发送到远程服务器并执行。如果 `ls` 和 `-l` 顺序颠倒，或者 `s` 丢了，命令就完全错了。
5. **Telnet (Telecommunication Network)** - 端口 23
   - **核心功能：** 不安全的远程命令行登录。
   - **为什么用TCP：** 理由和 SSH 完全一样。它现在基本被 SSH 淘汰了，因为它传输的数据是明文的，不安全。



#### 类别三：“重量级”数据类（数据库与路由）



这类应用需要传输大量且至关重要的数据，可靠性是基石。

1. **BGP (Border Gateway Protocol)** - 端口 179
   - **核心功能：** 互联网核心的**外部网关路由协议**。
   - **为什么用TCP：** 这是个关键对比（408考点）！**RIP 用 UDP** 是因为它在局域网内“吼一嗓子”（广播），简单粗暴；而 BGP 承载的是整个互联网的路由表，这些数据非常庞大且极其关键，必须在自治系统（AS）之间建立**稳定、可靠**的 TCP 连接来交换路由信息。
2. **DNS (Domain Name System)** - 端口 53
   - **核心功能：** 特殊情况下的 DNS 查询。
   - **为什么用TCP：** 这是个**特例**（408高频陷阱）。
     - **常规查询：** 默认用 **UDP**（快）。
     - **以下情况用 TCP：**
       1. **区域传输 (Zone Transfer):** 当一个备用 DNS 服务器要从主 DNS 服务器复制**整个**域名数据库时，这个数据量非常大，必须用 TCP 来保证可靠传输。
       2. **响应包过大：** 当一个 DNS 查询的响应包太大，超过了 UDP 包的大小限制时（通常是 512 字节，虽然现在有 EDNS 扩展），会转而使用 TCP。

------



### 总结（408高频记忆表）



| 协议名称 (缩写) | 常用端口 | 核心功能         | 为什么用TCP (归类)    |
| --------------- | -------- | ---------------- | --------------------- |
| **HTTP**        | 80       | 访问网页         | 零差错 (文件/资源)    |
| **HTTPS**       | 443      | 加密访问网页     | 零差错 (文件/资源)    |
| **FTP**         | 20 / 21  | 文件传输         | 零差错 (文件完整性)   |
| **SMTP**        | 25       | 发送邮件         | 状态同步 (邮件完整性) |
| **POP3**        | 110      | 接收邮件         | 状态同步 (邮件完整性) |
| **IMAP**        | 143      | 管理邮件         | 状态同步 (状态一致性) |
| **SSH**         | 22       | 安全远程登录     | 状态同步 (命令顺序)   |
| **Telnet**      | 23       | 远程登录(不安全) | 状态同步 (命令顺序)   |
| **BGP**         | 179      | 核心路由协议     | 重量级数据 (路由表)   |
| **DNS**         | 53       | 区域传输 / 大包  | 重量级数据 (特例)     |

导出到 Google 表格