这个观察非常敏锐，它不是巧合，而是二叉树的一个基本性质。408考研中，这正是一个经典的数据结构结论。

我们用两种方式来解释，一种是直观的“区间”法，另一种是严格的“二叉树性质”法。



### 1. 直观解释：n 个元素，n+1 个“缝隙”



折半查找的判定树，本质上是对一个有序序列进行区间划分。

- **成功结点（$n$ 个）：** 就是你数组里的 $n$ 个元素，我们称它们为 $k_1, k_2, ..., k_n$。
- **失败结点（$E$ 个）：** 代表查找失败的各种可能。

我们来看这 $n$ 个元素把“所有可能的数值”划分成了多少个区间：

1. 比 $k_1$ 还小

2. 在 $k_1$ 和 $k_2$ 之间

3. 在 $k_2$ 和 $k_3$ 之间

   ...

   n.  在 $k_{n-1}$ 和 $k_n$ 之间

   n+1. 比 $k_n$ 还大

**`_ (失败 E_0) _ [成功 k_1] _ (失败 E_1) _ [成功 k_2] _ (失败 E_2) _ ... _ [成功 k_n] _ (失败 E_n) _`**

你看，**$n$ 个“成功结点”作为“隔板”，不多不少，正好在数轴上隔出了 $n+1$ 个“失败区间”**（或叫“缝隙”）。

每一个“失败结点”就对应这样一个“缝隙”。

- 成功结点数 = $n$
- 失败结点数 = $n+1$

所以，失败结点数比成功结点数多 1。

------



### 2. 严格解释：扩充二叉树 (Extended Binary Tree) 的性质



这是408数据结构中更“科班”的解释。

折半查找判定树是一种特殊的二叉树，在数据结构中，我们称之为**“扩充二叉树”**（或“满二叉树”的另一种定义）。

1. **内部结点 (Internal Nodes)：** 对应你的**“成功结点”**。在判定树中，这些结点（圆圈）存储着数组中的元素 $k_i$。
2. **外部结点 (External Nodes)：** 对应你的**“失败结点”**。在判定树中，这些结点（通常画成方块）代表查找失败的终点，它们是树的“叶子”。

**核心性质：** 在折半查找判定树中，**每一个内部结点（成功结点）都恰好有两个孩子（分支）**。

- **为什么？** 因为每一次比较 `key` 和 `A[mid]`，只有三种结果：
  1. `key == A[mid]`：查找成功（停在内部结点）。
  2. `key < A[mid]`：走向左子树。
  3. `key > A[mid]`：走向右子树。
- 这个“左子树”和“右子树”，要么是**另一个内部结点**（代表子数组的`mid`），要么就是**一个外部结点**（代表子数组为空，查找失败）。
- 因此，每个代表 `A[mid]` 的内部结点，都必须引出两个分支，分别对应 `<` 和 `>` 的情况。

**二叉树的基本定理：**

> **在任何（非空）扩充二叉树中，若 $I$ 为内部结点的数量， $E$ 为外部结点的数量，则 $E = I + 1$。**

**【简单证明这个定理】**

- **初始状态：** 一棵树至少有1个内部结点（根结点）。根据定义，它必须有2个外部结点孩子。此时 $I=1, E=2$。$E = I + 1$ 成立。
- **归纳（“生长”）：** 我们如何让树“生长”？我们选择一个已有的“外部结点”（失败结点），将其**替换**为1个“内部结点”（成功结点）。
  - 我们 $I$ 增加了 1。（$I \rightarrow I+1$）
  - 我们 $E$ 减少了 1（被替换的那个）。
- 但根据定义，这个新加的“内部结点”又必须**自带 2 个**新的“外部结点”孩子。
  - 我们 $E$ 又增加了 2。
- **净变化：** $I$ 净增 1， $E$ 净增 1（$-1 + 2 = +1$）。
- **结论：** 无论树如何生长， $I$ 和 $E$ 始终保持“同步增 1”。它们从初始状态 $I=1, E=2$ 开始，这个 $E-I=1$ 的差值永远不变。

**应用到你的问题：**

- 成功结点数 = 内部结点数 $I = n$
- 失败结点数 = 外部结点数 $E$
- 根据定理 $E = I + 1$
- 所以，**失败结点数 = 成功结点数 + 1**