你提的 POP3 是一个很好的例子，但它**不属于** UDP 协议，而是恰好相反，它是**必须使用 TCP** 的典型。

- **POP3 (Post Office Protocol 3):** 这是用来**下载邮件**的。
- **为什么用 TCP？** 邮件是一种绝对要求**可靠传输**的数据。你肯定不希望你的邮件在下载过程中丢失一部分内容（比如附件损坏、正文缺失），或者邮件内容颠倒顺序。
- **结论：** POP3 使用的是 **TCP** (默认端口 110)，而不是 UDP，以确保每一封邮件都能完整、按顺序地被下载下来。

顺便提一下，其他的邮件协议，如 **SMTP** (用于发送邮件，端口 25) 和 **IMAP** (用于在服务器上管理邮件，端口 143)，也都是基于 **TCP** 的，因为它们都要求高可靠性。

------



### 尽可能多的梳理：使用 UDP 的协议



好的，按照你的要求，我来尽可能多地列举和梳理使用 UDP 的应用层协议（这在408中是高频考点），并帮你归类：

我还是按照我们之前总结的两大类来划分：



#### 类别一：“请求-响应”类（追求轻量、快速，应用层自己重试）



1. **DNS (Domain Name System)** - 端口 53
   - **核心功能：** 域名解析（如 `baidu.com` -> IP地址）。
   - **为什么用UDP：** 这是最典型的“请求-响应”场景。查询包和响应包都非常小，速度是第一位的。如果丢包，客户端（操作系统）自己会发起重试。
   - *(注意：DNS在特定情况下，如区域传输(Zone Transfer)时，会使用TCP，但常规查询默认用UDP)*
2. **DHCP (Dynamic Host Configuration Protocol)** - 端口 67 (Server), 68 (Client)
   - **核心功能：** 动态获取IP地址。
   - **为什么用UDP：** 客户端刚开机，连IP地址都没有，根本无法建立TCP连接。它必须通过UDP广播来寻找网络中的DHCP服务器。
3. **SNMP (Simple Network Management Protocol)** - 端口 161 (Agent), 162 (Manager)
   - **核心功能：** 网络设备管理。用于监控路由器、交换机等的运行状态。
   - **为什么用UDP：** “Simple”就体现在这里。网络管理通常需要轮询大量的设备，如果每个都建立TCP连接，开销巨大。它追求的是“轻量”和“广撒网”，不要求绝对可靠。
4. **TFTP (Trivial File Transfer Protocol)** - 端口 69
   - **核心功能：** 简单文件传输。
   - **为什么用UDP：** 它是FTP的“极简版”。“Trivial”（微不足道的）指的就是它非常小巧，代码简单。常用于局域网内网络设备（如路由器）备份或恢复配置文件。它在应用层实现了一种简单的“停止-等待”式的可靠性。
5. **NTP (Network Time Protocol)** - 端口 123
   - **核心功能：** 网络时间同步。
   - **为什么用UDP：** 时间同步对延迟非常敏感，需要快速的请求和响应来计算网络延迟，以便精确校准时间。TCP的连接建立和重传机制会引入不必要的延迟和抖动。



#### 类别二：“实时流”类（“过时不候”，宁可丢包不要卡顿）



1. **RTP (Real-time Transport Protocol)** - **(无固定端口)**
   - **核心功能：** **这是最重要的一个！** 专门用于承载实时数据，如视频流和音频流。
   - **为什么用UDP：** 这就是我们讨论的“过时不候”场景。视频会议、直播、VoIP电话，都是把音视频数据打包在RTP包里，然后RTP包再塞进UDP数据报里发送。它宁可丢掉几帧（画面花一下），也不要因为TCP重传导致整个画面卡住。
   - *(RTP本身不算应用层协议，它更像是和TCP/UDP同级的，但它封装在UDP之上)*
2. **RIP (Routing Information Protocol)** - 端口 520
   - **核心功能：** 一种较早的动态路由协议（属于应用层，但服务于网络层）。
   - **为什么用UDP：** 它采用“周期性广播”的策略，比如每30秒，路由器就把自己的整个路由表通过UDP广播给所有邻居。这是一种“尽力而为”的更新，不需要建立TCP连接。



#### 类别三：（进阶）在UDP上“重建”可靠性



1. **QUIC (Quick UDP Internet Connections)**
   - **核心功能：** 这是 **HTTP/3** 使用的传输协议。
   - **为什么用UDP：** 这是一个非常现代的设计。TCP有一个“队头阻塞”问题（一个包丢了，后面的包就算到了也得等着）。QUIC为了解决这个问题，干脆**绕过TCP**，直接在UDP之上，由应用层自己实现了**可靠传输、拥塞控制、多路复用**等功能。它吸取了UDP的“快”（无连接），又自己实现了TCP的“稳”。

------



### 总结（408高频记忆表）



| 协议名称 (缩写) | 常用端口  | 核心功能      | 为什么用UDP (归类)       |
| --------------- | --------- | ------------- | ------------------------ |
| **DNS**         | 53        | 域名解析      | 请求-响应，轻量快速      |
| **DHCP**        | 67 / 68   | 动态获取IP    | 请求-响应，无法建TCP     |
| **TFTP**        | 69        | 简单文件传输  | 请求-响应，协议简单      |
| **SNMP**        | 161 / 162 | 网络管理      | 请求-响应，开销小        |
| **NTP**         | 123       | 时间同步      | 请求-响应，延迟敏感      |
| **RTP**         | (无)      | 传输音视频流  | 实时流，过时不候         |
| **RIP**         | 520       | 路由信息协议  | 周期性广播，尽力而为     |
| **QUIC**        | (无)      | HTTP/3 的基础 | (进阶) 在UDP上重建可靠性 |

导出到 Google 表格